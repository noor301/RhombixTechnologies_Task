.confirm-badge {
  width: 56px; height: 56px;
  border-radius: 50%;
  background: var(--basil);
  color: var(--white);
  display: flex; align-items: center; justify-content: center;
  font-size: 26px;
  margin: 0 auto 16px;
}
.confirm-title { text-align: center; font-size: 28px; }
.confirm-sub { text-align: center; color: #6d5d4c; margin-bottom: 28px; }

.confirm-number {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 14px;
}
.confirm-number strong { font-size: 16px; color: var(--chili); }

.confirm-item {
  display: flex;
  justify-content: space-between;
  padding: 6px 0;
  font-size: 14.5px;
}

.confirm-address { font-size: 14px; margin: 4px 0; color: #5b4c3c; }

.confirm-actions {
  display: flex;
  gap: 12px;
  margin-top: 24px;
}
